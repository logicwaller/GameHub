# 网页游戏管理网站（GameHub）完整实施方案

## 一、项目概述

### 1.1 项目定位
一个类似"谜页集"（miyeji.cn）的网页游戏聚合平台，核心功能是收录、展示和管理外部网页游戏链接，为用户提供便捷的游戏发现、分类浏览、搜索和收藏服务，并集成AI游戏攻略助手。

### 1.2 核心业务场景
| 用户角色 | 核心场景 |
|---------|---------|
| **访客** | 浏览首页推荐、按分类/标签筛选游戏、搜索游戏、查看游戏详情、跳转游玩 |
| **注册用户** | 收藏游戏、查看个人收藏列表、使用AI攻略助手提问 |
| **管理员** | 后台管理游戏（增删改查）、管理分类/标签、审核游戏链接有效性 |

---

## 二、技术架构总览

### 2.1 技术栈清单

| 层级 | 技术组件 | 版本/说明 |
|------|---------|----------|
| **前端框架** | Vue 3 |
| **后端框架** | Go + Gin | 高性能HTTP服务 |
| **数据库** | MySQL 8.0 | 主数据存储 |
| **缓存/状态** | Redis 7.0 | 会话、排行榜、缓存、限流 |
| **消息队列** | KafKa | 异步任务、解耦 |
| **向量数据库** | 待定 | RAG知识库存储 |
| **LLM服务** | 待定 | 攻略问答生成 |
| **反向代理** | Nginx | 负载均衡、静态资源 |

### 2.2 系统架构图（逻辑分层）

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                             │
│   Web浏览器 (Vue 3 SPA) → Nginx反向代理 → 后端API          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        接入层                               │
│   Gin Router → JWT认证中间件 → 限流中间件 → 日志中间件      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        业务层                               │
│   Handler → Service → Repository 三层架构                   │
│   包含：用户模块、游戏模块、收藏模块、管理模块、AI模块       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据层                               │
│   ┌──────┬──────┬──────┬──────┬──────┬──────┐             │
│   │MySQL │Redis │Rabbit│  ES  │Chroma│  OSS │             │
│   │(主库)│(缓存)│ (MQ) │(搜索)│(向量)│(图片)│             │
│   └──────┴──────┴──────┴──────┴──────┴──────┘             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 数据流向说明

| 操作场景 | 数据流向 |
|---------|---------|
| **用户浏览游戏列表** | 前端 → API → Service → MySQL（分页查询）→ 返回 |
| **用户查看游戏详情** | 前端 → API → Service → Redis缓存（命中即返回）→ MySQL（未命中查库并写缓存） |
| **用户搜索游戏** | 前端 → API → Service → 返回ID列表 → MySQL补充详情 |
| **用户点击"开始游玩"** | 前端 → API → Service → 发送MQ消息（异步）→ 返回成功 → 消费者累加MySQL点击数 + 更新Redis排行榜 |
| **用户收藏游戏** | 前端 → API → Service → MySQL写入收藏表 → 更新Redis收藏数缓存 |
| **用户提问AI攻略** | 前端 → API → Service → 向量检索 → 拼接Prompt → API → 流式返回 |

---

## 三、核心功能模块详细设计

### 3.1 用户认证与权限模块

| 功能点 | 技术实现 | 数据存储 |
|-------|---------|---------|
| 注册/登录 | JWT双Token机制（Access Token 2h + Refresh Token 7d） | MySQL `users`表存储bcrypt加密密码 |
| 路由守卫 | 前端Vue Router全局前置守卫 + 后端中间件JWT校验 | Redis存储黑名单（注销/改密时失效） |
| 权限控制 | RBAC模型（admin / user 两种角色） | MySQL `users.role`字段 + Casbin策略（可选） |

### 3.2 游戏管理模块

| 功能点 | 技术实现 | 数据存储 |
|-------|---------|---------|
| 游戏录入 | 管理员后台表单提交（标题、链接、缩略图、描述、分类、标签） | MySQL `games`表 |
| 游戏列表 | 分页 + 多条件筛选（分类、标签、是否精选） | MySQL + Redis缓存首页热门数据 |
| 游戏详情 | 展示完整信息 + 收藏状态 + 游玩链接 | Redis缓存详情页（TTL 1小时） |
| 游戏点击统计 | 异步MQ处理，避免阻塞主流程 | MySQL `play_count`字段 + Redis ZSet排行榜 |

### 3.3 分类与标签模块

- **分类**：一级分类（如：动作、策略、休闲、解谜），支持后台CRUD
- **标签**：多标签体系（如：多人、单机、像素风、高难度），支持后台CRUD
- **关联关系**：游戏-分类（多对多）、游戏-标签（多对多），通过中间表实现

### 3.4 用户收藏模块

- 用户可收藏/取消收藏游戏
- 个人中心展示收藏列表（分页）
- 收藏数实时更新（同步写MySQL + 异步更新Redis计数缓存）

### 3.5 AI攻略助手模块（RAG）

| 阶段 | 工作内容 | 技术组件 |
|------|---------|---------|
| **知识库构建（离线）** | 管理员录入攻略文本 → 文本分块（500 token/块）→ 向量化 → 存入DB | 离线Python脚本 或 Go调用Embedding API |
| **实时问答（在线）** | 用户提问 → 问题向量化 → DB相似检索（Top 3）→ 拼接Prompt → 调用API生成回答 → 流式SSE返回 | Go后端 + API + DB |

---

## 四、各中间件的具体职责

### 4.1 Redis 职责清单

| 数据类型 | Key设计示例 | 用途 | 过期策略 |
|---------|------------|------|---------|
| String | `user:token:{userID}` | 用户会话Token黑名单 | 7天 |
| String | `game:detail:{id}` | 游戏详情页缓存 | 1小时 |
| String | `game:featured` | 精选游戏列表JSON | 10分钟 |
| String | `rate:limit:{ip}` | API限流计数器 | 60秒 |
| Hash | `game:stats:{id}` | 游戏实时统计数据（点击数、收藏数） | 永久 |
| Sorted Set | `game:hot:rank` | 热门游戏排行榜（分数=点击数） | 永久 |
| Sorted Set | `game:fav:rank` | 收藏榜（分数=收藏数） | 永久 |

### 4.2 KafKa 职责清单

| 队列名称 | 生产者触发场景 | 消费者职责 | 重试机制 |
|---------|-------------|-----------|---------|
| `queue.game.play` | 用户点击"开始游玩"时 | 异步累加MySQL点击数 + 更新Redis排行榜 | 失败重试3次后进入死信队列 |
| `queue.search.sync` | 管理员新增/修改/删除游戏时 | 同步更新索引 | 失败重试5次，记录错误日志 |
| `queue.notification` | 用户注册成功 / 游戏审核通过 | 发送邮件通知 | 失败重试3次 |

### 4.3 互动与统计场景的缓存、削峰策略

第三阶段的中间件设计覆盖游戏、评论和论坛等全部高频路径。需要立即反馈且必须强一致的数据先写入 MySQL；允许最终一致、可能出现突发流量或需要通知/统计的数据通过 Kafka 削峰；Redis 只保存可重建的缓存、计数和短期状态。

| 业务操作 | Redis 处理 | Kafka 处理 | 数据一致性要求 |
|---------|-----------|-----------|---------------|
| 游戏浏览/点击 | `game:stats:{id}` 计数、`game:hot:rank` 排行榜、短时用户/IP 去重 | `game.play` 事件，消费者批量累加游玩量 | 消费者事务写入 `games.plays` 和 `game_play_events`，支持重试和幂等 |
| 游戏详情/列表 | `game:detail:{id}`、`games:list:*`，随机 TTL 防雪崩 | 游戏变更发送 `search.sync`，异步更新索引和清理缓存 | MySQL 是唯一真实来源，变更后删除缓存 |
| 点赞/收藏 | 实时计数和用户关系集合，加速状态查询 | `interaction.event` 用于统计和通知，不替代关系表 | MySQL 唯一键事务写入，成功后更新或失效缓存 |
| 游戏评论 | 最近评论短 TTL 缓存、评论数计数 | `comment.moderation` 审核，`notification` 通知 | 评论正文先写 MySQL，回复数和评论数以数据库为准 |
| 论坛发帖/回复 | `forum:latest`、`forum:hot`、`post:replies:{id}` 短 TTL | `search.sync`、`notification`、`comment.moderation` | 正文同步写 `posts`/`post_replies`；回复数使用 `COUNT(*)` 或事务计数 |
| 创作者数据分析 | `analytics:{user}:{range}` 短 TTL，实时计数 Hash | `analytics.event` 异步聚合小时/日指标 | 定时任务将聚合结果写入统计表 |

### 4.4 Redis 的其他适用功能

- 登录会话、JWT 黑名单和刷新令牌状态（设置过期时间）。
- API 分布式限流、验证码和登录失败次数限制。
- 热门搜索词、分类排行榜、最近浏览列表（Sorted Set/List）。
- 防重复提交和分布式锁，例如游戏创建和点赞接口幂等控制。
- AI 攻略热门问题答案缓存，降低模型调用成本。

### 4.5 Kafka 的其他适用功能

- 评论审核、举报处理、站内通知和邮件通知。
- 游玩、点赞、收藏、评论等行为分析及日报/周报聚合。
- 外部游戏链接检测、排行榜刷新等耗时后台任务。
- 管理员操作审计日志，以及失败消息的重试和死信队列。

### 4.6 中间件使用原则

1. Kafka 事件携带唯一 `event_id`，消费者必须幂等，重复消费不能重复累加。
2. Redis 失效或重启后，排行榜和计数可从 MySQL/事件表重建，不能把 Redis 作为唯一数据源。
3. 评论、帖子正文和点赞/收藏关系优先保证 MySQL 持久化；Kafka 主要用于削峰、审核、通知和统计。
4. 各队列配置重试次数、死信队列和监控指标，关注积压量、消费延迟和失败率。

---

## 五、AI攻略助手的完整工作流

### 5.1 知识库构建流程（管理员侧）

```
步骤1：管理员在后台录入游戏攻略文本（支持富文本编辑器）
    ↓
步骤2：后端接收到文本后，调用文本分块服务
    ↓
步骤3：将每个文本块调用Embedding模型转为向量
    ↓
步骤4：将向量 + 元数据（game_id, chunk_index, 标题）存入DB
    ↓
步骤5：记录处理状态到MySQL（`game_knowledge`表）
```

### 5.2 用户问答流程（实时）

```
步骤1：用户在前端输入问题
    ↓
步骤2：后端接收请求，将问题文本转为向量
    ↓
步骤3：在DB中检索与当前游戏ID匹配的Top 3相似知识片段
    ↓
步骤4：将检索到的片段 + 用户问题 拼接成系统Prompt
    ↓
步骤5：调用API（流式SSE），实时推送给前端展示
    ↓
步骤6：完整回答生成后，将问答记录存入MySQL（用于后续分析优化）
```

---

## 六、分阶段实施计划

### 第一阶段：基础框架搭建（第1-2周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| 项目初始化（Monorepo结构） | `backend/` + `frontend/` 目录 | 全栈 |
| 前后端基础配置 | Go Gin框架 + Vue3 Vite项目启动 | 前后端 |
| 前端路由 + 布局 | 首页、游戏列表页、详情页骨架 | 前端 |
| 用户认证API | 注册/登录接口 + JWT中间件 | 后端 |
| 前端登录/注册页面 | 表单交互 + Token存储（localStorage） | 前端 |

**里程碑**：用户可注册登录，前端页面骨架可访问。

---

### 第二阶段：核心业务开发（第3-5周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| 游戏CRUD API | 增删改查接口 + 管理员权限校验 | 后端 |
| 游戏列表前端 | 分页 + 分类/标签筛选 + 卡片展示 | 前端 |
| 游戏详情页 | 展示完整信息 + "开始游玩"按钮跳转 | 前端 |
| 分类/标签管理 | 后台管理界面 + API | 前后端 |
| 用户收藏功能 | 收藏/取消收藏 + 个人收藏列表 | 前后端 |
| 后台管理界面 | 游戏录入表单 + 列表管理 + 分类标签管理 | 前端 |

**里程碑**：管理员可在后台录入游戏，用户可在前台浏览和收藏。

---

### 第三阶段：性能优化与中间件集成（第6-7周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| Redis缓存集成 | 游戏详情页缓存 + 首页精选缓存 | 后端 |
| Redis排行榜 | 热门游戏实时排行榜API + 前端展示 | 前后端 |
| Kafka集成 | 游戏点击统计异步处理 | 后端 |
| 前端搜索页 | 关键词搜索 + 高亮展示 + 分页 | 前端 |
| API限流 | 基于Redis的分布式限流中间件 | 后端 |

**里程碑**：系统具备高性能（缓存）、高可用（异步）和智能搜索能力。

---

### 第四阶段：AI攻略助手开发（第8-9周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| DB部署 | 连接配置 | 后端 |
| 知识库构建工具 | 文本分块 + 向量化 + 入库脚本 | 后端/AI |
| 攻略录入后台 | 管理员在游戏详情页录入攻略文本 | 前后端 |
| 问答API开发 | 向量检索 + Prompt拼接 + AI调用 | 后端 |
| AI助手前端组件 | 对话式UI + 流式SSE接收 | 前端 |
| 问答记录存储 | MySQL记录用户问答日志 | 后端 |

**里程碑**：用户可在游戏详情页向AI助手提问并获取攻略回答。

---

## 七、风险与应对策略

| 风险项 | 影响等级 | 应对策略 |
|-------|---------|---------|
| **外部游戏链接失效** | 中 | 增加定期链接检测任务（每天扫描一次），标记失效游戏 |
| **Redis缓存雪崩/击穿** | 高 | 采用不同过期时间（随机偏移）+ 布隆过滤器防穿透 |
| **DeepSeek API调用成本** | 中 | 设置每日Token限额 + 缓存高频问题答案 |
| **ES索引与MySQL不一致** | 中 | 采用MQ最终一致性 + 每日定时全量同步兜底 |
| **恶意爬虫/刷榜** | 中 | 加强限流策略 + 排行榜只统计登录用户行为 |
