# 网页游戏管理网站（GameHub）完整实施方案

## 一、项目概述

### 1.1 项目定位
一个类似"谜页集"（miyeji.cn）的网页游戏聚合平台，核心功能是收录、展示和管理外部网页游戏链接，为用户提供便捷的游戏发现、分类浏览、搜索和收藏服务，并集成AI游戏攻略助手。

### 1.2 核心业务场景
| 用户角色 | 核心场景 |
|---------|---------|
| **访客** | 浏览首页推荐、按分类/标签筛选游戏、搜索游戏、查看游戏详情、跳转游玩 |
| **注册用户** | 收藏游戏、查看个人收藏列表、使用AI攻略助手提问 |
| **管理员** | 后台管理游戏（增删改查）、管理分类/标签、审核游戏链接有效性 |

---

## 二、技术架构总览

### 2.1 技术栈清单

| 层级 | 技术组件 | 版本/说明 |
|------|---------|----------|
| **前端框架** | Vue 3 |
| **后端框架** | Go + Gin | 高性能HTTP服务 |
| **数据库** | MySQL 8.0 | 主数据存储 |
| **缓存/状态** | Redis 7.0 | 会话、排行榜、缓存、限流 |
| **消息队列** | KafKa | 异步任务、解耦 |
| **向量数据库** | 待定 | RAG知识库存储 |
| **LLM服务** | 待定 | 攻略问答生成 |
| **反向代理** | Nginx | 负载均衡、静态资源 |

### 2.2 系统架构图（逻辑分层）

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                             │
│   Web浏览器 (Vue 3 SPA) → Nginx反向代理 → 后端API          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        接入层                               │
│   Gin Router → JWT认证中间件 → 限流中间件 → 日志中间件      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        业务层                               │
│   Handler → Service → Repository 三层架构                   │
│   包含：用户模块、游戏模块、收藏模块、管理模块、AI模块       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据层                               │
│   ┌──────┬──────┬──────┬──────┬──────┬──────┐             │
│   │MySQL │Redis │Kafka │本地索引│Chroma│  OSS │             │
│   │(主库)│(缓存)│ (MQ) │(搜索) │(向量)│(图片)│             │
│   └──────┴──────┴──────┴──────┴──────┴──────┘             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 数据流向说明

| 操作场景 | 数据流向 |
|---------|---------|
| **用户浏览游戏列表** | 前端 → API → Service → MySQL（分页查询）→ 返回 |
| **用户查看游戏详情** | 前端 → API → Service → Redis缓存（命中即返回）→ MySQL（未命中查库并写缓存） |
| **用户搜索游戏** | 前端 → API → Service → 返回ID列表 → MySQL补充详情 |
| **用户点击"开始游玩"** | 前端 → API → Service → 发送MQ消息（异步）→ 返回成功 → 消费者累加MySQL点击数 + 更新Redis排行榜 |
| **用户收藏游戏** | 前端 → API → Service → MySQL写入收藏表 → 更新Redis收藏数缓存 |
| **用户提问AI攻略** | 前端 → API → Service → 向量检索 → 拼接Prompt → API → 流式返回 |

---

## 三、核心功能模块详细设计

### 3.1 用户认证与权限模块

| 功能点 | 技术实现 | 数据存储 |
|-------|---------|---------|
| 注册/登录 | JWT双Token机制（Access Token 2h + Refresh Token 7d） | MySQL `users`表存储bcrypt加密密码 |
| 路由守卫 | 前端Vue Router全局前置守卫 + 后端中间件JWT校验 | Redis存储黑名单（注销/改密时失效） |
| 权限控制 | RBAC模型（admin / user 两种角色） | MySQL `users.role`字段 + Casbin策略（可选） |

### 3.2 游戏管理模块

| 功能点 | 技术实现 | 数据存储 |
|-------|---------|---------|
| 游戏录入 | 创作者后台表单提交（标题、链接、缩略图、描述、主类型、标签） | MySQL `games`、`tags`、`game_tags`表 |
| 游戏列表 | 分页 + 主类型筛选、标签展示、关键词搜索 | MySQL + Redis缓存首页热门数据 |
| 游戏详情 | 展示完整信息 + 收藏状态 + 游玩链接 | Redis缓存详情页（TTL 1小时） |
| 游戏点击统计 | 异步MQ处理，避免阻塞主流程 | MySQL `play_count`字段 + Redis ZSet排行榜 |

### 3.3 主类型与标签模块

- **主类型**：每个游戏必须选择一个主类型：`ARG/WIG`、`现实互动解谜`、`网页互动游戏`、`网页解谜`、`互动叙事`。
- **标签**：每个游戏可添加最多 12 个自由标签，用于描述题材、玩法、媒介或体验提示，例如“中式民俗”“档案推理”“互动视频”“跨设备”“轻恐”。
- **关联关系**：主类型存储在 `games.primary_type`；标签使用 `tags` 与 `game_tags` 多对多关联表，避免同一标签被重复存储。

### 3.4 用户收藏模块

- 用户可收藏/取消收藏游戏
- 个人中心展示收藏列表（分页）
- 收藏数实时更新（同步写MySQL + 异步更新Redis计数缓存）

### 3.5 AI攻略助手模块（RAG）

| 阶段 | 工作内容 | 技术组件 |
|------|---------|---------|
| **知识库构建（离线）** | 管理员录入攻略文本 → 文本分块（500 token/块）→ 向量化 → 存入DB | 离线Python脚本 或 Go调用Embedding API |
| **实时问答（在线）** | 用户提问 → 问题向量化 → DB相似检索（Top 3）→ 拼接Prompt → 调用API生成回答 → 流式SSE返回 | Go后端 + API + DB |

---

## 四、各中间件的具体职责

### 4.1 Redis 职责清单（已实现）

| 数据类型 | Key 设计示例 | 当前用途 | 过期策略 |
|---------|-------------|----------|----------|
| String | `game:detail:{id}` | 游戏详情缓存 | 1 小时 |
| String | `games:list:::plays`、`games:list:::likes` | 默认游戏列表、点赞排序列表缓存 | 10 分钟 |
| String | `game:comments:{id}` | 游戏评论列表缓存 | 2 分钟 |
| String | `forum:latest`、`post:detail:{id}` | 论坛列表、帖子详情缓存 | 2 分钟 |
| String | `rate:limit:{ip}`、`auth:limit:{ip}` | 通用 API 限流、登录注册限流 | 60 秒 |
| String | `auth:blacklist:{tokenHash}` | 退出登录后的 JWT 黑名单 | 至 Token 到期 |
| String | `idempotency:{ip}:{key}` | 带 `Idempotency-Key` 的 POST 请求防重复提交 | 2 分钟 |
| Hash | `game:stats:{id}` | 游戏浏览、点赞、收藏、评论数的快速读取统计 | 不过期，可由 MySQL 重建 |
| Sorted Set | `game:hot:rank` | 按游玩量排序的热门游戏榜 | 不过期，可由 MySQL 重建 |
| Sorted Set | `game:fav:rank` | 按收藏量排序的收藏榜 | 不过期，可由 MySQL 重建 |
| Set | `user:like:{userID}`、`user:fav:{userID}` | 当前用户的点赞、收藏游戏 ID 集合 | 不过期，可由 MySQL 重建 |

Redis 使用 Cache Aside（旁路缓存）策略：读取时先查 Redis，未命中时查 MySQL 并回填；写操作先成功写入 MySQL，再更新统计或删除关联缓存。Redis 不可用时，读取会回退到 MySQL。

### 4.2 Kafka 职责清单（已实现）

| Topic | 生产者触发场景 | 当前消费者职责 | 失败处理 |
|-------|----------------|----------------|----------|
| `game.play` | 用户点击“开始游玩” | 事务写入 `game_play_events`、更新 `games.plays`、更新 Redis 热门榜、聚合每日游玩量 | 重试 3 次；失败写入 `game.play.dlq`；Kafka 不可用时接口回退为同步 MySQL 写入 |
| `search.sync` | 创建游戏、发布帖子 | 更新 MySQL `search_documents` 本地搜索文档表 | 重试 3 次；失败写入 `search.sync.dlq` |
| `interaction.event` | 点赞、收藏、游戏评论 | 聚合 `game_analytics_daily` 中的每日点赞、收藏、评论数据 | 重试 3 次；失败写入 `interaction.event.dlq` |
| `notification` | 用户注册、游戏评论、帖子回复 | 写入 `notifications` 站内通知表 | 重试 3 次；失败写入 `notification.dlq` |

每个事件都有唯一 `event_id`。消费者通过 `kafka_processed_events` 表防止重复消费；业务主题和对应死信主题由后端启动时自动确保创建。

### 4.3 当前已实现的数据流

| 业务操作 | MySQL 处理 | Redis 处理 | Kafka 处理 |
|---------|------------|-----------|-----------|
| 游戏浏览/点击 | 消费者写入游玩事件、累加游玩量；Kafka 不可用时在同一 MySQL 事务内同步写入游玩事件、游玩量和日统计 | 更新热门榜、游戏统计，失效游戏详情和列表缓存 | 发布并消费 `game.play` |
| 游戏详情、游戏列表 | MySQL 是缓存未命中时的真实来源 | 缓存详情和默认排序列表 | 无 |
| 点赞、收藏 | 同步写关系表和游戏计数 | 更新统计 Hash、收藏榜、用户关系 Set，失效列表/详情缓存 | 发布 `interaction.event` 聚合每日数据 |
| 游戏评论 | 同步写评论和评论数 | 失效评论、详情和列表缓存，更新统计 Hash | 发布互动统计和通知事件 |
| 论坛发帖 | 同步写帖子 | 失效论坛列表缓存 | 发布 `search.sync` |
| 帖子回复 | 同步写回复 | 失效论坛列表、帖子详情缓存 | 发布通知事件 |
| 收藏排行榜 | MySQL 作为 Redis 未命中时的兜底数据源 | 读取/重建 `game:fav:rank` | 无 |
| 创作者数据页 | 读取 `games` 与 `game_analytics_daily` | 无 | 由游玩与互动消费者持续聚合；首次启动时从历史业务表回填 |

### 4.4 已实现的使用原则

1. MySQL 是用户、游戏、帖子、评论、点赞和收藏关系的唯一真实来源；Redis 仅保存可重建的缓存和派生状态。
2. 点赞、收藏、评论、发帖等需要立即确认且不能丢失的操作，先同步写入 MySQL，再发布 Kafka 派生事件。
3. 游戏游玩量允许短暂最终一致，通过 Kafka 异步处理；Kafka 不可用时回退为同步 MySQL 写入。
4. Redis 缓存失效或重启后，可从 MySQL 与游玩事件表重建；Kafka 消费端通过 `event_id` 防止重复累计。
5. `analytics_backfill_state` 用于确保历史日统计只回填一次。游玩量可由 `game_play_events` 精确回填；旧点赞、收藏和已删除评论缺少完整变更日志，只能按现存记录近似回填。

---

## 五、AI攻略助手的完整工作流

### 5.1 知识库构建流程（管理员侧）

```
步骤1：管理员在后台录入游戏攻略文本（支持富文本编辑器）
    ↓
步骤2：后端接收到文本后，调用文本分块服务
    ↓
步骤3：将每个文本块调用Embedding模型转为向量
    ↓
步骤4：将向量 + 元数据（game_id, chunk_index, 标题）存入DB
    ↓
步骤5：记录处理状态到MySQL（`game_knowledge`表）
```

### 5.2 用户问答流程（实时）

```
步骤1：用户在前端输入问题
    ↓
步骤2：后端接收请求，将问题文本转为向量
    ↓
步骤3：在DB中检索与当前游戏ID匹配的Top 3相似知识片段
    ↓
步骤4：将检索到的片段 + 用户问题 拼接成系统Prompt
    ↓
步骤5：调用API（流式SSE），实时推送给前端展示
    ↓
步骤6：完整回答生成后，将问答记录存入MySQL（用于后续分析优化）
```

---

## 六、分阶段实施计划

### 第一阶段：基础框架搭建（第1-2周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| 项目初始化（Monorepo结构） | `backend/` + `frontend/` 目录 | 全栈 |
| 前后端基础配置 | Go Gin框架 + Vue3 Vite项目启动 | 前后端 |
| 前端路由 + 布局 | 首页、游戏列表页、详情页骨架 | 前端 |
| 用户认证API | 注册/登录接口 + JWT中间件 | 后端 |
| 前端登录/注册页面 | 表单交互 + Token存储（localStorage） | 前端 |

**里程碑**：用户可注册登录，前端页面骨架可访问。

---

### 第二阶段：核心业务开发（第3-5周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| 游戏CRUD API | 增删改查接口 + 管理员权限校验 | 后端 |
| 游戏列表前端 | 分页 + 分类/标签筛选 + 卡片展示 | 前端 |
| 游戏详情页 | 展示完整信息 + "开始游玩"按钮跳转 | 前端 |
| 分类/标签管理 | 后台管理界面 + API | 前后端 |
| 用户收藏功能 | 收藏/取消收藏 + 个人收藏列表 | 前后端 |
| 后台管理界面 | 游戏录入表单 + 列表管理 + 分类标签管理 | 前端 |

**里程碑**：管理员可在后台录入游戏，用户可在前台浏览和收藏。

---

### 第三阶段：性能优化与中间件集成（第6-7周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| Redis缓存集成 | 游戏详情、列表、评论、论坛与帖子详情缓存 | 后端 |
| Redis排行榜与状态 | 热门榜、收藏榜、游戏统计、用户点赞收藏状态、JWT 黑名单和限流 | 前后端 |
| Kafka集成 | 游玩异步统计、搜索文档同步、互动日统计和站内通知 | 后端 |
| 前端搜索页 | 关键词搜索 + 高亮展示 + 分页 | 前端 |
| API保护 | 基于 Redis 的限流、退出 Token 黑名单和可选幂等键 | 后端 |

**里程碑**：系统具备 Redis 缓存和限流能力，Kafka 能异步处理游玩与派生业务事件，并提供真实的创作者日统计数据。

---

### 第四阶段：AI攻略助手开发（第8-9周）

| 任务 | 交付物 | 负责人参考 |
|------|-------|----------|
| DB部署 | 连接配置 | 后端 |
| 知识库构建工具 | 文本分块 + 向量化 + 入库脚本 | 后端/AI |
| 攻略录入后台 | 管理员在游戏详情页录入攻略文本 | 前后端 |
| 问答API开发 | 向量检索 + Prompt拼接 + AI调用 | 后端 |
| AI助手前端组件 | 对话式UI + 流式SSE接收 | 前端 |
| 问答记录存储 | MySQL记录用户问答日志 | 后端 |

**里程碑**：用户可在游戏详情页向AI助手提问并获取攻略回答。

---

## 七、风险与应对策略

| 风险项 | 影响等级 | 应对策略 |
|-------|---------|---------|
| **外部游戏链接失效** | 中 | 增加定期链接检测任务（每天扫描一次），标记失效游戏 |
| **Redis缓存不可用或过期** | 高 | 缓存未命中或 Redis 不可用时回退到 MySQL；缓存只保存可重建数据 |
| **DeepSeek API调用成本** | 中 | 设置每日Token限额 + 缓存高频问题答案 |
| **本地搜索文档与MySQL不一致** | 中 | 通过 `search.sync` 异步更新 `search_documents`；MySQL 游戏列表仍可作为查询兜底 |
| **恶意爬虫/刷榜** | 中 | 加强限流策略 + 排行榜只统计登录用户行为 |
